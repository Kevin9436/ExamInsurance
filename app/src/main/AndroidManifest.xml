<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kevinlee.examinsurance">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.Entrance_activity"
            android:label="Exam Insurance"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.Login_activity"
            android:label="Login"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.Register_activity"
            android:label="Register"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.ShopPage_activity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.UserPage_activity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.UserInfo_activity" />
        <activity android:name=".activity.UserAccount_activity" />
        <activity android:name=".activity.UserHistory_activity" />
        <activity android:name=".activity.UserInstruction_activity"></activity>
    </application>

</manifest>